function replaceWithDad() {
    var inBox = (document.getElementById('Ishanifier').value);
    inBox = inBox.replace(/ /g, "DAD");
    document.getElementById('Ishanifier').value = inBox;
}
// calvert was here